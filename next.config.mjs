/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "cdn.pixabay.com",
      "placehold.co",
      "cdn-icons-png.flaticon.com",
      "images.unsplash.com",
    ],
  },
};

export default nextConfig;
